#include<stdio.h>
#include <math.h>

int main () {

    float n;
    printf("Enter a number for finding squar of it : ");
    scanf("%f",&n);
    printf("Squar of number = %f",pow(n,2));

    /*float s;
    s = pow (n,2);
    printf("Squar of number = %f",s);
    */
    return 0;
}