#include <stdio.h>
#include <math.h>

int main () {

    int n;
    printf("Enter sequence Number : "); 
    scanf("%d", &n);

    float squarroot;
    squarroot = sqrt(n);
    printf("Square root is %f\n", squarroot );
    

    return 0;
}